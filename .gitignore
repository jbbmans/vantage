node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.DS_Store
playwright-report/
test-results/
data/

# Scratch directory for the walkthrough recorder; the finished files land in public/videos/.
.video-work/
.ssr-build/

# Screenshots and other local QA output. Inspected, never committed.
artifacts/

# Graphify knowledge graph of the code. Generated locally with `graphify extract . --code-only`; never committed.
graphify-out/
